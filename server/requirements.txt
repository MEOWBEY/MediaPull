yt-dlp==2026.6.9
# Browser TLS/HTTP impersonation backend used by yt-dlp's `impersonate` option
# to bypass anti-bot 403/410 on sites like tiktok.
curl_cffi==0.13.0
# Image/gallery downloader (invoked as a subprocess, never imported — it's
# GPL-3.0, subprocess isolation keeps this app's own license independent).
gallery-dl==1.30.0
httpx==0.28.1
fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.7.1
gunicorn==26.0.0
python-multipart==0.0.32
